@model List<WebApplicationAPP.Models.Sinpe>

@{
    ViewData["Title"] = "Historial SINPE";
}

<h2>Historial de Pagos</h2>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Telefono Origen</th>
            <th>Caja</th>
            <th>Monto</th>
            <th>Descripcion</th>
            <th>Fecha</th>
            <th>Estado</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.TelefonoOrigen</td>
                <td>@item.Caja.NombreMostrado</td>
                <td>@item.Monto</td>
                <td>@item.Descripcion</td>
                <td>@item.FechaDeRegistro</td>
                <td>
                    @(item.Estado ? "Sincronizado" : "No sincronizado")
                </td>
            </tr>
        }
    </tbody>
</table>
